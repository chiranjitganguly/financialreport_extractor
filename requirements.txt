# LLM framework
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Document conversion (primary)
docling>=2.0.0

# Raw text fallback
PyMuPDF>=1.27.0

# Language detection
lingua-language-detector>=2.0.0

# Fuzzy string matching
rapidfuzz>=3.0.0

# Schema validation & config
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async Postgres driver
asyncpg>=0.30.0

# Sync Postgres driver (SQLAlchemy engine used by Agent 3 + Alembic)
psycopg2-binary>=2.9.0

# SQLAlchemy ORM + Alembic migrations (Agent 3+)
sqlalchemy>=2.0.0
alembic>=1.13.0

# pgvector extension support + LangChain vector store
pgvector>=0.3.0
langchain-postgres>=0.0.12

# API server (HITL review endpoint)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# OpenAI client (transitive via langchain-openai, pinned for stability)
openai>=1.0.0

# Safe expression evaluation for validation rules (sandboxed — no raw eval())
simpleeval>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
